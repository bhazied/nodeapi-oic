{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "npm run build && nodemon ./dist/app.js",
    "start": "npm run build && node ./dist/server.js",
    "build": "rimraf ./dist && babel --presets es2015 -d dist/ . --ignore ./node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.18.9",
    "rimraf": "^2.6.3"
  }
}
